# ─── Production Dependencies ───────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Validation & Config
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Email
fastapi-mail>=1.4.1

# Payment
stripe>=9.0.0
razorpay>=1.4.1  # Legacy — kept for backward compatibility

# HTTP client (Ollama provider, tests)
httpx>=0.27.0

# Logging
structlog>=24.0.0

# Redis (chat history)
redis[asyncio]>=5.0.0

# Vector DB
chromadb>=0.5.0

# AI/LLM Providers
google-generativeai>=0.7.0
groq>=0.9.0

# Embeddings & RAG
sentence-transformers>=3.0.0
pypdf>=4.0.0
langchain-text-splitters>=0.2.0

# ─── Development Dependencies ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.0
